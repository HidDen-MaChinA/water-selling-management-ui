import type React from "react";

export type BlazeBaseTHeadType = (
  props: React.DetailedHTMLProps<
    React.HtmlHTMLAttributes<HTMLTableSectionElement>,
    HTMLTableSectionElement
  >
) => React.ReactNode;
